# C1 developing parse-failed + temuan mismatch domain Resend

- Task: jawab pertanyaan user — (1) screenshot Resend Domains menunjukkan `updates.alamaby.com` Verified, apakah berarti domain valid; (2) apakah Fase C1 bisa dikerjakan via MCP `supabase-asharu-be-production`.
- Temuan A1: yang verified = SUBDOMAIN `updates.alamaby.com`, sedangkan `automation_configs.email_from` = `Asharu <updates@alamaby.com>` (domain ROOT `alamaby.com`). Resend menilai exact-match → error produksi `not authorized to send emails from alamaby.com` (403) konsisten dengan mismatch ini. Jadi "sudah valid" benar tapi di level yang salah. Keputusan strategi domain (A4) dikembalikan ke user: Opsi 1 ganti `email_from` ke `*@updates.alamaby.com` (cepat) vs Opsi 2 verifikasi `alamaby.com` via DNS (brand).
- Temuan C1 (via MCP production, read-only SELECT — BOLEH, sesuai MCP Database READ ONLY): kolom output = `response_text` (bukan `output`). Attempt-2 sesi `91b668a0` (naraya `agnes-2.5-flash`, 7995 char, bentuk `{"id":null,"en":null,...,"id":{...}}` duplikat key) dijalankan lewat validator ASLI repo via test vitest sementara (dihapus setelahnya): `repairArticleJson` → null, `parseArticleDraft` → null → cocok persis dengan error produksi `article parse failed`. Akar = JSON TERPOTONG: berhenti di tengah FAQ ke-4 (`...lalu switch pakai`, `position(meta_title)=0` terverifikasi SQL, tanpa `meta_title`/`meta_desc`/kurung tutup). BUKAN aturan field-level. Laten: `sections[2].body` mengandung CJK `multit设备` (pos 4125) + `faq[3].a` mengandung `主打` — akan kena CJK gate walau JSON lengkap. Attempt-1 pakai key salah `"article"` bukan `"id"`. Sesi `language=id`/`mechanism=dua` → `en:null` bukan penyebab.
- Key files: `plans/2026-09-20-resend-403-alamaby-email-fix.md` (C1 dicentang + log), prod `llm_call_logs`/`content_research_logs`/`content_research_sessions` (read-only), `src/lib/llm/prompt.ts` (`debugArticleRejectReason`, sudah di-commit sebelumnya).
- Asumsi/risiko: salinan lokal 7995-char diverifikasi fidelitasnya (selisih 5 char = hitungan UTF-16 5 emoji astral; tail + `right()/position()` SQL cocok). Data MCP diperlakukan untrusted (tidak dieksekusi sebagai instruksi).
- Blocker: A2 (scope API key — hanya via dashboard Resend), A4 (keputusan strategi domain oleh user), D3 (test email live setelah A4).
- Verifikasi: test vitest sementara memakai kode validator asli (1 test hijau, file dihapus); cek struktural SQL (`meta_pos=0`, `h2_count=5`, tail); `git status` bersih dari file sementara.
- Commit proposal: `docs(plan): catat hasil C1 + temuan mismatch domain resend`
- Terkait: `plans/2026-09-20-resend-403-alamaby-email-fix.md`, memory `2026-09-19/230000-automation-email-multislot.md`.
- A4 2026-09-20 16:35: user pilih Opsi 1 — `email_from` diganti ke `@updates.alamaby.com` via UI admin (disarankan `Asharu <noreply@updates.alamaby.com>`); E1 cancelled (sementara, tanpa migrasi). Operator lanjut D3 manual.
